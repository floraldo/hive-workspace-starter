# Apps

An app is a stable thing a person can run or open to complete a workflow: a small web tool,
dashboard, command-line utility, or desktop interface.

Create one only after the workflow, users, inputs, outputs, and approval boundaries are
understood. Keep reusable logic in a package rather than duplicating it in the interface.

This folder is intentionally empty at first.
